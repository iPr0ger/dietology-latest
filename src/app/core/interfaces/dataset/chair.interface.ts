export interface ChairRequestInterface {
  vk_id: string;
  faculty_id: string;
  title: string;
}

export interface ChairResponseInterface {
  id: string;
  vk_id: string;
  faculty_id: string;
  title: string;
}
