export interface LocationRequestInterface {
  vk_id: string;
  title: string;
  area: string;
  region: string;
}

export interface LocationResponseInterface {
  id: string;
  vk_id: string;
  title: string;
  area: string;
  region: string;
}
